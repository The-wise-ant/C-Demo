#include <iostream>
#include <cmath>

using std::cout;

int main()
{

  cout << "The higher number between 3 and 2 is " << std::max(3, 2) << std::endl;
  cout << "The lower number between 3 and 2 is " << std::min(3, 2) << std::endl;

  return 0;
}